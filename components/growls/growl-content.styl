@require '~styles/variables'
@require '~styles-lib/mixins'

.growl-inner
	h4
		margin: 0
		font-weight: 300

	p
		margin-bottom: 9px
		font-size: $font-size-small

	a
		color: $dark-theme-link
		text-decoration: underline

		&:hover
			color: $dark-theme-link-hover

	.progress
		margin-bottom: 9px

	p:last-child, .progress:last-child
		margin-bottom: 0

.growl-type-success
	.growl-title
		theme-prop('color', 'highlight')

.growl-type-error
	.growl-title
		theme-prop('color', 'notice')

.growl-type-info
	.growl-title
		color: $white
